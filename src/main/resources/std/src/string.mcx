import {
  int::-,
  int::=,
}

export builtin def size: Proc String -> Int;

export const def repeat: Proc Tuple(String, Int) -> string =
  if count = 0 then "" else string ++ (repeat @ (string, count - 1));

export def to_string: Proc String -> String = $(identity @ `String`);
