export def not : proc (a : bool) -> bool :=
  \(a) -> if a then false else true;

export def and : proc (a : bool, b : bool) -> bool :=
  \(a, b) -> if a then b else false;

export def or : proc (a : bool, b : bool) -> bool :=
  \(a, b) -> if a then true else b;
