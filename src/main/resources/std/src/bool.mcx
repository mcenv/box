export def not : func (bool) -> bool :=
  \(a) -> if a then false else true;

export def and : func (bool, bool) -> bool :=
  \(a, b) -> if a then b else false;

export def or : func (bool, bool) -> bool :=
  \(a, b) -> if a then true else b;
